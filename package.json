{
    "name": "form-steve",
    "version": "1.0.0",
    "description": "Formulario para Cámara de Comercio de Samborondón",
    "main": "index.js",
    "scripts": {
        "build": "pip install -r requirements.txt && mkdir -p static/templates && cp -r app/templates/* static/templates/ && cp -r app/static/* static/"
    },
    "engines": {
        "node": ">=16.0.0"
    },
    "dependencies": {
        "fs": "0.0.1-security",
        "path": "^0.12.7"
    }
}